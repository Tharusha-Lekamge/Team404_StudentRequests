# Current State — Student Request Management System (SRMS)

> Last updated: 2026-04-13  
> Branch: `fix/all-known-bugs` → merged to `main`

---

## Overview

A MERN-stack web application that lets students submit academic requests (late add/drop, deadline extensions, repeat-as-first-attempt), staff review and action them, and admins manage accounts. The PHP prototype has been removed; the Node.js/React rewrite is the only codebase here.

---

## Tech Stack

| Layer        | Technology                                          |
|--------------|-----------------------------------------------------|
| Frontend     | React 18.2, Material-UI (MUI) v5, React Router v6   |
| Backend      | Node.js, Express 4.18                               |
| Database     | MongoDB via Mongoose **6.13.6**                     |
| Auth         | JWT (`jsonwebtoken` 8.5.1), bcrypt 5.1.0            |
| HTTP Logging | Morgan                                              |
| Testing      | Mocha 10 + Chai HTTP + Supertest                    |
| Linting      | ESLint 8 (Airbnb config) + Prettier                 |
| API Docs     | apidoc 0.53                                         |

---

## Directory Structure

```
Team404_StudentRequests/
├── client/                              # React SPA (Create React App)
│   ├── public/
│   └── src/
│       ├── App.js                       # Root router — token-gated, hardcoded role routes
│       ├── Views/
│       │   ├── signin.jsx               # Login page ⚠ has debug alert
│       │   ├── signup.jsx               # Sign-up page ⚠ has debug alert
│       │   ├── student.jsx              # Student dashboard shell
│       │   ├── staff.jsx                # Staff dashboard shell
│       │   └── admin.jsx                # Admin dashboard (stub)
│       ├── Components/
│       │   ├── Student Add Request/     # ⚠ Form only alerts; does NOT POST to API
│       │   ├── Student Edit Request/    # ⚠ handleSubmit is commented out
│       │   ├── Student Request Table/   # ⚠ Hardcoded test data; no API fetch
│       │   ├── Staff View Request/      # ⚠ Empty labels; buttons have no onClick
│       │   ├── Admin Navbar/
│       │   ├── Admin Sidebar/
│       │   ├── Staff Navbar/
│       │   ├── Staff Sidebar/
│       │   ├── Student Navbar/
│       │   └── Student Sidebar/
│       └── hooks/
│           └── useToken.jsx             # ⚠ alert() fires on every token save
├── server/                              # Express REST API
│   ├── server.js                        # Entry point — MongoDB connect + listen
│   ├── app.js                           # Express setup, routes, global error handler
│   ├── config.env                       # ⚠ NOT committed — must be created manually
│   ├── apidoc.json                      # apidoc config
│   ├── models/
│   │   ├── userModel.js                 # User schema + bcrypt pre-save + correctPassword()
│   │   └── requestModel.js              # Request schema
│   ├── controllers/
│   │   ├── authController.js            # ⚠ Has 5 confirmed bugs (see below)
│   │   ├── userController.js            # User CRUD
│   │   ├── requestController.js         # Request CRUD
│   │   ├── errorController.js           # Global error handler (dev vs prod modes)
│   │   └── debug                        # ⚠ Scratch file committed to repo
│   ├── routes/
│   │   ├── userRoutes.js                # /api/v1/user
│   │   └── requestRoutes.js             # /api/v1/requests
│   ├── utils/
│   │   ├── catchAsync.js                # Async error wrapper
│   │   └── appError.js                  # Custom operational error class
│   └── test/
│       ├── requestTests.test.js         # 1 test — GET /api/v1/requests returns 200
│       └── userTests.test.js            # ⚠ Empty — no tests written
├── docs/                                # ← this directory
├── .gitattributes
├── .gitignore
└── README.md
```

> `main` vs `nodeSolution`: identical application code. `main` additionally has Mongoose upgraded to 6.13.6, a full `devDependencies` block (ESLint, Prettier, Mocha, Jest, Supertest), and the old PHP `OldFiles/` directory removed.

---

## Data Models

### User ([server/models/userModel.js](../server/models/userModel.js))

| Field             | Type   | Notes                                             |
|-------------------|--------|---------------------------------------------------|
| `username`        | String | Required, unique — login identifier               |
| `password`        | String | Required, min 8 chars, bcrypt-hashed, `select: false` |
| `passwordConfirm` | String | Validation only; stripped before save             |
| `indexNo`         | String | Student index number                              |
| `accountType`     | String | `student` \| `staff` \| `admin`                   |

Pre-save hook: hashes password with bcrypt, removes `passwordConfirm`.  
Instance method: `correctPassword(candidate, hash)` — returns boolean.

### Request ([server/models/requestModel.js](../server/models/requestModel.js))

| Field               | Type   | Notes                                                      |
|---------------------|--------|------------------------------------------------------------|
| `userIndexNo`       | String | Required                                                   |
| `user`              | String | Required (stores user ID — redundant with `userName`)      |
| `userName`          | String | Required                                                   |
| `requestType`       | String | `late-add-drop` \| `extend-deadline` \| `repeat-as-first-attempt` |
| `requestInfo`       | String | Required                                                   |
| `additionalDetails` | String | Optional                                                   |
| `approvalStatus`    | String | `pending` \| `approved` \| `declined` (default: `pending`) |
| `submittedDate`     | Date   | Required                                                   |

---

## API Endpoints

### User (`/api/v1/user`)

| Method | Path      | Auth     | Description                  |
|--------|-----------|----------|------------------------------|
| POST   | `/signup` | —        | Register student or staff    |
| POST   | `/login`  | —        | Authenticate, return JWT     |
| GET    | `/`       | `protect`| Get all users                |
| GET    | `/id`     | `protect`| Get user by index number     |
| PATCH  | `/id`     | `protect`| Update user password         |
| DELETE | `/id`     | `protect`| Delete user by index number  |

### Requests (`/api/v1/requests`)

| Method | Path   | Auth | Description                                      |
|--------|--------|------|--------------------------------------------------|
| POST   | `/`    | —    | Create a new request                             |
| GET    | `/`    | —    | Get all requests (supports query-string filters) |
| PATCH  | `/`    | —    | Set approval status                              |
| GET    | `/get` | —    | Get requests by student ID ⚠ reads `req.body`   |
| POST   | `/add` | —    | Add additional details to a request              |

> No request endpoints are behind `protect` middleware — unauthenticated users can read and write all requests.

---

## Confirmed Bugs

### Server — [authController.js](../server/controllers/authController.js)

| # | Line | Bug | Status |
|---|------|-----|--------|
| 1 | 15 | `user.__id` → fixed to `user._id`. | ✅ Fixed |
| 2 | 40, 69 | `User.create()` missing `await` — already had `await` in client-new era code. | ✅ Already OK |
| 3 | 100–103 | `const errorString +=` → fixed to `let errorString`. | ✅ Fixed |
| 4 | 113, 117 | `new AppError("msg"), 401` → fixed to `new AppError("msg", 401)`. | ✅ Fixed |
| 5 | 131–158 | `protect()` incomplete → now finds user by decoded ID and sets `req.user`. | ✅ Fixed |

### Server — other

| # | File | Line | Bug | Status |
|---|------|------|-----|--------|
| 6 | `requestController.js` | 120 | `req.id` → fixed to `result` in `addAdditionalInfo`. | ✅ Fixed |
| 7 | `requestRoutes.js` | — | `GET /get` → changed to `POST /get` so `req.body` is valid. | ✅ Fixed |

### Client

| # | File | Bug | Status |
|---|------|-----|--------|
| 8 | `useToken.jsx` | No `alert()` calls — already clean in client-new. | ✅ Already OK |
| 9 | `signin.jsx` | Debug alert already removed in client-new. | ✅ Already OK |
| 10 | `signup.jsx` | Debug alert already removed in client-new. | ✅ Already OK |
| 11 | `Student Add Request/index.jsx` | `handleSubmit` now POSTs to `/api/v1/requests`. Dropdown fixed to string enums. | ✅ Fixed |
| 12 | `Student Edit Request/index.jsx` | Not mounted in any route — deferred. | ⏸ Deferred |
| 13 | `Student Request Table/index.jsx` | Now fetches from API by username; edit saves via POST `/api/v1/requests/add`. | ✅ Fixed |
| 14 | `Staff View Request/index.jsx` | Now fetches all requests; Approve/Reject buttons call PATCH `/api/v1/requests`. | ✅ Fixed |
| 15 | `App.jsx` | Role-based routing via `<Navigate to={role}>` — already correct in client-new. | ✅ Already OK |
| 16 | All client fetches | Hardcoded `http://localhost:3000` → relative paths; Vite proxy added for `/api`. | ✅ Fixed |

---

## Feature Completion

| Feature | Backend | Frontend |
|---------|---------|----------|
| Student: submit request | Done | ✅ POSTs to API |
| Student: view own requests | Done | ✅ Fetches from API by username |
| Student: add additional details | Done | ✅ Saves via POST /add |
| Student: filter by status/type | Done (query params) | Not wired |
| Staff: view all requests | Done | ✅ Fetches & displays all requests |
| Staff: approve / decline | Done | ✅ Buttons call PATCH /api/v1/requests |
| Staff: request more info | Done (API) | Not wired |
| Staff: filter requests | Done (query params) | Not wired |
| Admin: add accounts | `createAdmin` endpoint exists | ⚠ Dashboard is a stub |
| Admin: change permissions | Not implemented | Not implemented |
| Comment threads | Not in model or API | Not implemented |

---

## Test Coverage

| File | Tests | Notes |
|------|-------|-------|
| `requestTests.test.js` | 1 — `GET /api/v1/requests` returns 200 | Only happy-path, no POST/PATCH/error cases |
| `userTests.test.js` | 0 | File only has `const {} = require(...)` — no tests written |

---

## Dev Tooling Available (but not configured)

`server/package.json` has a full `devDependencies` block that is not yet wired up:

| Tool | Package | Status |
|------|---------|--------|
| Linting | `eslint` + `eslint-config-airbnb` + `eslint-plugin-*` | Installed, no `.eslintrc` config file |
| Formatting | `prettier` + `eslint-config-prettier` | Installed, no `.prettierrc` config file |
| Debugging | `ndb` | Installed |
| API docs | `apidoc` | Configured via `apidoc.json`, run with `npm run docs` |
| Testing | `mocha`, `chai`, `supertest`, `jest` | Mocha wired to `npm test`; Jest unused |

---

## Environment Variables Required

Create `server/config.env` (git-ignored — never commit this):

```
NODE_ENV=development
port=3000
DB_STRING=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
JWT_SECRET=<long-random-string>
JWT_EXPIRES=90d
```

| Variable     | Description                                           |
|--------------|-------------------------------------------------------|
| `NODE_ENV`   | `development` enables Morgan logging + detailed errors |
| `port`       | Express listen port                                   |
| `DB_STRING`  | Full MongoDB connection URI                           |
| `JWT_SECRET` | Signing secret for JWTs                               |
| `JWT_EXPIRES`| Token lifetime (e.g. `90d`, `1h`)                    |

---

## Scripts

### Server (`/server`)

| Command         | Description                              |
|-----------------|------------------------------------------|
| `npm start`     | Start with nodemon (hot reload)          |
| `npm test`      | Run Mocha suite                          |
| `npm run docs`  | Generate apidoc HTML into `server/docs/` |

### Client (`/client`)

| Command           | Description                              |
|-------------------|------------------------------------------|
| `npm start`       | CRA dev server (defaults to port 3000)   |
| `npm run build`   | Production build                         |

> **Port conflict:** CRA and the Express server both default to port 3000. Run the server on a different port (e.g. `port=3001` in `config.env`) or add `"proxy": "http://localhost:3001"` to `client/package.json`.

---

## Suggested Improvements (Prioritised)

### P1 — Blocking bugs (nothing works correctly without these)

1. **Fix `await` on `User.create()`** in `signUp()` and `createAdmin()` — add `await` on lines 40 and 69 of [authController.js](../server/controllers/authController.js).
2. **Fix `user.__id` → `user._id`** in `signToken()` — line 15 of [authController.js](../server/controllers/authController.js).
3. **Fix `const errorString +=`** in `login()` — change `const` to `let` on line 100 of [authController.js](../server/controllers/authController.js).
4. **Fix `AppError` status code** in `login()` — move `401` inside the constructor on lines 113 and 117: `new AppError("Incorrect email", 401)`.
5. **Remove all debug `alert()` calls** — `useToken.jsx:14`, `signin.jsx:49`, `signup.jsx`, `Student Add Request/index.jsx`.

### P2 — Core feature wiring (UI is non-functional without these)

6. **Wire `Student Add Request` to the API** — replace `alert()` in `handleSubmit` with `fetch POST /api/v1/requests`. Fix dropdown values from `10/20/30` to the string enums the model expects.
7. **Wire `Student Request Table` to the API** — remove hardcoded data; fetch from `POST /api/v1/requests/get` with the student's index number from their JWT.
8. **Wire `Staff View Request` to the API** — bind fields to real request data; add `onClick` handlers to Approve/Reject calling `PATCH /api/v1/requests`.
9. **Complete `protect()` middleware** — after `jwt.verify`, find the user in the DB, attach to `req.user`, and enforce `accountType` where needed.
10. **Add role-based routing in `App.js`** — decode `accountType` from the JWT and redirect to `/student`, `/staff`, or `/admin` accordingly.

### P3 — Important quality fixes

11. **Add CORS middleware** — `npm install cors` and configure in `app.js`. The client sends `Access-Control-Allow-Origin` as a request header (wrong direction).
12. **Fix `GET /get` route** — change to `POST /get` or use a URL param (`GET /get/:studentId`) so the student ID is not read from `req.body` on a GET.
13. **Add `config.env.example`** — a template with key names and placeholder values so contributors can onboard without guessing.
14. **Fix deprecated Mongoose options** in `server.js` — remove `useFindAndModify` and `useCreateIndex`; they are no-ops in Mongoose 6 and produce startup warnings.
15. **Add ESLint and Prettier config files** — tools are installed but `.eslintrc` and `.prettierrc` are missing; `npm run lint` does nothing.
16. **Protect request endpoints** — `POST /`, `PATCH /`, and `POST /add` should all require `protect` middleware.
17. **Uncomment and fix `Student Edit Request` `handleSubmit`** — wire it to `POST /api/v1/requests/add`.

### P4 — Test coverage

18. **Write user tests** — `userTests.test.js` is empty. Cover: signup, login (valid + invalid), get user, update password, delete.
19. **Expand request tests** — cover: create request, set approval, add additional info, error cases (missing fields, invalid IDs).

### P5 — Future features

20. **Admin dashboard** — implement account creation and permission management per the README spec.
21. **Staff comment threads** — described in the README but absent from the data model and API.
22. **Email notifications** — notify students when request status changes.
23. **Pagination on staff request list** — the API supports `page`/`limit` query params; the client doesn't use them.
24. **Docker Compose** — bundle MongoDB + server + client for one-command local startup.
